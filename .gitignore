node_modules/
dist/
.build/
*.log
.DS_Store

# Xcode derived data / build output for the packaging project
packaging/.dd/
*.xcarchive
.env
.env.*

# never commit real keys or enrolled signers
*.pub
allowed_signers
id_*
*_sk
# App Store Connect / signing secrets — never commit
*.p8
AuthKey_*.p8
*.p12
*.cer
